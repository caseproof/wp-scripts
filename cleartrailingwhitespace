echo "/usr/bin/perl -p -i -e 's/\s+$/\n/g' `find . -regextype posix-extended -iregex "\./(vendor|script|test).*" -prune -o -name \"*.php\"`"
/usr/bin/perl -p -i -e 's/\s+$/\n/g' `find . -regextype posix-extended -iregex "\./(vendor|script|test).*" -prune -o -name "*.php"`
echo "/usr/bin/perl -p -i -e 's/\s+$/\n/g' `find . -regextype posix-extended -iregex "\./(vendor|script|test).*" -prune -o -name \"*.css\"`"
/usr/bin/perl -p -i -e 's/\s+$/\n/g' `find . -regextype posix-extended -iregex "\./(vendor|script|test).*" -prune -o -name "*.css"`
echo "/usr/bin/perl -p -i -e 's/\s+$/\n/g' `find . -regextype posix-extended -iregex "\./(vendor|script|test).*" -prune -o -name \"*.js\"`"
/usr/bin/perl -p -i -e 's/\s+$/\n/g' `find . -regextype posix-extended -iregex "\./(vendor|script|test).*" -prune -o -name "*.js"`

